[package]
name = "combat"
version = "0.1.0"
edition = "2021"

[lints]
workspace = true

[dependencies]
bevy.workspace = true
models = { path = "../models" }
derive_more = { version = "2.1.1", features = ["full"] }
